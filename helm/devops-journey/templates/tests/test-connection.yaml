apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-journey.fullname" . }}-test-connection"
  labels:
    {{- include "devops-journey.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-journey.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
